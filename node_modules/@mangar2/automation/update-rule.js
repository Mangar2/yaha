module.exports = (message) => {
    const { topic, value } = message
    const chunks = topic.split('/')
    if (chunks[0] === '$SYS' && chunks[1] === 'automation' && chunks.at(-1) === 'set') {
        chunks.pop()
        chunks.splice(0, 2)
        const rulePath = chunks.join('/')
    }
}