#!/usr/bin/env node

/**
 * @license
 * This software is licensed under the GNU LESSER GENERAL PUBLIC LICENSE Version 3. It is furnished
 * "as is", without any support, and with no warranty, express or implied, as to its usefulness for
 * any purpose.
 *
 * @author Volker Böhm
 * @copyright Copyright (c) 2020 Volker Böhm
 * @overview
 * Main program, service to store messages and provide a rest api to access to stored messages an the message hierarchy
 * This service is implemented by connecting the mqttclient and the messagestore to a new service
 */

'use strict'

const MqttClient = require('@mangar2/mqttclient')
const MessageStore = require('@mangar2/messagestore')

// path to your configuration file
const getConfiguration = require('./configuration')

/**
 * @description
 * Configuration of the messageservice and the message store
 * Provide a file called "yahaconfig.json" in the installation root to change configuration.
 * Use the configuration file to configure
 * multiple services. It supports multiple environments set by the NODE_ENV setting ('development', 'test', 'staging', 'production')
 * All configuration settings have default values. You do not have to change any of them.
 */
const config = getConfiguration('yahaconfig.json')

const mqttClient = new MqttClient(config.messageService)
mqttClient.run()

/**
 * Register messageStore
 */
const messageStore = new MessageStore(config.messageStore)
messageStore.run()
mqttClient.registerRecipient(config.messageService.subscriptions, (message) => {
    return messageStore.addMessage(message)
})
mqttClient.on('shutdown', async () => {
    messageStore.close()
})
console.log('started service messageStore')

module.exports = mqttClient
