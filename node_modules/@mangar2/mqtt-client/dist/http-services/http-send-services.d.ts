/**
 * @license
 * This software is licensed under the GNU LESSER GENERAL PUBLIC LICENSE Version 3. It is furnished
 * "as is", without any support, and with no warranty, express or implied, as to its usefulness for
 * any purpose.
 *
 * @author Volker Böhm
 * @copyright Copyright (c) 2020 Volker Böhm
 * @overview Provides functions to connect to the (http based) yaha mqtt broker
 */
import { IMessage } from '@mangar2/mqtt-utils';
import { BrokerOptions, headers_t } from '../service/types';
/**
 * Provides functions to send data to the broker
 */
export declare class HttpSendServices {
    private _httpClient;
    private _publish;
    /**
     * Creates a new http service
     * @param port the port on which the server listens
     */
    constructor(brokerOptions: BrokerOptions);
    /**
     * Sends an HTTP request.
     *
     * @param path - The path of the request.
     * @param method - The HTTP method of the request.
     * @param headers - The headers of the request.
     * @param payload - The payload of the request.
     * @returns A promise that resolves to the response of the request.
     */
    send(path: string, headers: headers_t, payload: any): Promise<import("@mangar2/httpservice/dist/client").Response>;
    /**
     * Publishes a message
     */
    publish(token: string, message: IMessage, serviceName: string, version?: string): Promise<string[]>;
}
