/**
 * @license
 * This software is licensed under the GNU LESSER GENERAL PUBLIC LICENSE Version 3. It is furnished
 * "as is", without any support, and with no warranty, express or implied, as to its usefulness for
 * any purpose.
 *
 * @author Volker Böhm
 * @copyright Copyright (c) 2020 Volker Böhm
 * @name Shutdown
 * @description
 * Shutdown is a small helper to safely shutdown serivces on SIGINT (ctrl-C)
 * A singelton calling a callback on SIGINT and provides a timeout if the callback does not finish in a period of time.
 * @example
 * const shutdown = require('shutdown');
 *
 * shutdown(async () => {
 *    await myClass.close();
 *    process.exit(0);
 * });
 */
'use strict'

const FORCE_SHUTDOWN_TIMEOUT_IN_MILLISECONDS = 4000

let sigintCallback
let timeout

/**
 * Registers a callback to be called on SIGINT (ctrl-C), with a timeout for forced shutdown.
 *
 * @param {function} callback - The function to be called on SIGINT.
 * @param {number} [forceShutdownTimeoutInMilliseconds=4000] - The amount of milliseconds until forced shutdown.
 */
module.exports = (callback, forceShutdownTimeoutInMilliseconds = FORCE_SHUTDOWN_TIMEOUT_IN_MILLISECONDS) => {
    timeout = forceShutdownTimeoutInMilliseconds

    sigintCallback = callback

    process.on('SIGINT', () => {
        if (sigintCallback !== undefined) {
            sigintCallback()
        }

        setTimeout((err) => {
            if (err) {
                console.error(err)
            }
            process.exit(1)
        }, timeout)
    })

    process.on('SIGTERM', () => {
        process.exit(0)
    })
}
